To Start the backend server follow the below steps:
- clone the repository of the Task 1 folder.
- navigate to the API folder.
- do "npm install" to install nodemodules and other necessary libraries.
- do "npm start" to run the backend server ( make sure you have mongodb).
- the server is running on local host 5000
